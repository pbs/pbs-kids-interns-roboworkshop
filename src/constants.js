export const GAMEPLAY = Object.freeze(
{
    WIDTH: 1320,
    HEIGHT: 780
});

// enum for each body part, to be used for determining the roboParts' and toolboxes' types
export const BODYPARTS = Object.freeze(
{
    HEAD: 1, 
    BODY: 2, 
    ARM: 3, 
    LEG: 4
});